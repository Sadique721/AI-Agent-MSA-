# MSA AI Agent — Python Dependencies
# =====================================

# Core
numpy
pandas

# Web Server (primary)
flask
flask-socketio
python-socketio

# HTTP / Web search
requests
beautifulsoup4

# Speech Recognition (offline, Vosk)
vosk

# Text-to-Speech
pyttsx3

# Audio I/O
sounddevice
soundfile

# Computer Vision
opencv-python

# Encryption / Security
cryptography

# System Monitoring
psutil

# Deep Learning (optional — Speaker Verification)
torch
torchaudio
transformers
sentencepiece
speechbrain

# Whisper STT (optional, heavy)
# openai-whisper

# Local LLM via GGUF (optional — needs model file)
# llama-cpp-python

# Android Device Control (optional)
adb-shell
pure-python-adb

# System Automation (optional)
pyautogui
pynput

# Web scraping
selenium

# ── NEW: Upgrade 1 — Language Engine ─────────────────────────────────────────
# Statistical language detection (English / Hindi / Hinglish)
langdetect

# ── NEW: Upgrade 3 — RAG Memory ──────────────────────────────────────────────
# Offline semantic embeddings (~80 MB model, downloaded once)
sentence-transformers
# CPU-only FAISS vector index (no GPU required)
faiss-cpu

# ── NEW: Upgrade 4 — Browser Agent ───────────────────────────────────────────
# Reliable browser automation (Chromium/Firefox/WebKit)
playwright

# Speech recognition (wake-word + listen loop)
SpeechRecognition

# Cloud LLM Support
google-generativeai

